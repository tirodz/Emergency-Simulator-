# Windows PE version resource, embedded into the executable by PyInstaller.
#
# Why this file exists: an unsigned executable with no version resource gives a reputation-based
# scanner no positive identity to weigh against the fact that it unpacks and runs code. That
# combination is what the Trojan:Win32/Bearfoos class of machine-learning detections keys on. The
# fields below make the binary identify itself and its purpose on inspection.
#
# Kept in sync with app/__init__.py:__version__ by tools/check_version_info.py.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo([
      StringTable(
        '040904B0',
        [
          StringStruct('CompanyName', 'Emergency-Simulator project'),
          StringStruct('FileDescription', 'Emergency-Simulator - Android test-alert controller'),
          StringStruct('FileVersion', '1.0.0.0'),
          StringStruct('InternalName', 'Emergency-Simulator'),
          StringStruct('LegalCopyright', 'See the repository for licensing'),
          StringStruct('OriginalFilename', 'Emergency-Simulator.exe'),
          StringStruct('ProductName', 'Emergency-Simulator'),
          StringStruct('ProductVersion', '1.0.0.0'),
          StringStruct(
            'Comments',
            'Triggers a TEST emergency alert on a rooted Android device you control, '
            'through the device\'s own Cell Broadcast pipeline. Performs no cellular '
            'transmission. https://github.com/tirodz/Emergency-Simulator-'
          ),
        ]
      )
    ]),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)